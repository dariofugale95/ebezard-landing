# Production Dockerfile for eBezard Landing
FROM node:20-alpine AS builder
WORKDIR /app
COPY . .
RUN npm install --production && npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 8088
CMD ["nginx", "-g", "daemon off;"]